$kind: http-request
url: "{{baseUrl}}/workflow/approveRequest/{{approverId}}?key=pip_plan"
method: POST
headers:
  Content-Type: application/json
queryParams:
  key: pip_plan
body:
  type: text
  content: |-
    {
      "approvalId": "workflow-approval-uuid-here",
      "status": "COMPLETED",
      "remarks": "PIP plan looks good. Approved."
    }
order: 3000
