$kind: http-request
url: "{{baseUrl}}/pip/{{pipId}}/comments"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "comment": "Good progress on code review turnaround. Keep it up.",
      "isPrivate": false
    }
order: 1000
