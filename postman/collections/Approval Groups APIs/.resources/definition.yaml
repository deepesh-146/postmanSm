$kind: collection
description: API collection for managing approval groups in HRMS workflow system
variables:
  baseUrl: http://localhost:3000
  authToken: your-auth-token-here
  groupId: ""
auth:
  - id: 887981c4-5843-4aca-916e-e1cce29508a7
    type: bearer
    name: bearer auth
    credentials:
      token: "{{orgToken}}"
