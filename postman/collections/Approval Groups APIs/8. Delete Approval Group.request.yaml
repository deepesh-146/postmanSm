$kind: http-request
description: Delete an approval group (only if not used in active workflows)
url: "{{baseUrl}}/api/approval-groups/{{groupId}}"
method: DELETE
headers:
  Authorization: Bearer {{authToken}}
order: 8000
