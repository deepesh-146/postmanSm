$kind: http-request
description: Lock a single employee's appraisal within a cycle.
url: "{{baseUrl}}/cycle/{{cycleId}}/employee/{{employeeId}}/lock"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
        "reason": "Employee score finalized after calibration meeting"
    }
order: 2000
