$kind: http-request
description: Finalize ALL scores → FINALIZED. Creates PENDING acknowledgement for each employee. Auto-locks the cycle.
url: "{{baseUrl}}/appraisal-finalization/cycle/{{cycleId}}/finalize"
method: POST
order: 1000
