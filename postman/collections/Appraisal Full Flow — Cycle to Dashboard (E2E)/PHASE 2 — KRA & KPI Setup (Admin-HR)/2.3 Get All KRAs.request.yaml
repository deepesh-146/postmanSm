$kind: http-request
description: List all KRAs. Note the IDs.
url: "{{baseUrl}}/goals/kra/all?page=0&size=10"
method: GET
queryParams:
  - key: page
    value: "0"
  - key: size
    value: "10"
  - key: employeeId
    value: "{{employeeId}}"
    disabled: true
order: 3000
