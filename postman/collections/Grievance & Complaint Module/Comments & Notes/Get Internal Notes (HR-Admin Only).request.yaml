$kind: http-request
name: Get Internal Notes (HR/Admin Only)
description: Returns all internal notes. Accessible only to HR, Admin, or the assigned handler.
url: "{{base_url}}/grievances/{{grievanceId}}/internal-notes"
method: GET
order: 4000
