$kind: http-request
description: Cancel a DRAFT cycle and unlink all bonuses
url: "{{base_url}}/api/bonus/payout-cycles/:id/cancel"
method: POST
headers:
  Authorization: Bearer {{access_token}}
pathVariables:
  - key: id
    value: "{{cycle_id}}"
    description: Cycle UUID
order: 9000
