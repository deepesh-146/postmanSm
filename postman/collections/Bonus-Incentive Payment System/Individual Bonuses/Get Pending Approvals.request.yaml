$kind: http-request
description: Get all pending bonus approvals for current user (Manager/HR/Finance)
url: "{{base_url}}/api/bonus/pending-approvals"
method: GET
headers:
  Authorization: Bearer {{access_token}}
order: 7000
