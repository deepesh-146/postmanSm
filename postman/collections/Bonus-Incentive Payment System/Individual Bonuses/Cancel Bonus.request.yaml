$kind: http-request
description: Cancel a bonus (only if not paid)
url: "{{base_url}}/api/bonus/:id/cancel"
method: POST
headers:
  Authorization: Bearer {{access_token}}
pathVariables:
  - key: id
    value: "{{bonus_id}}"
    description: Bonus UUID
order: 8000
