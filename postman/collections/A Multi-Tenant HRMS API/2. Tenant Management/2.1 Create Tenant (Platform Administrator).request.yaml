$kind: http-request
description: Create a new tenant. Requires SUPERADMIN role.
url: "{{hrmsBaseUrl}}/tenants"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "clientId": "falcon",
      "name": "Falcon Group",
      "email": "admin@falcon.com",
      "password": "5684"
    }
order: 1000
