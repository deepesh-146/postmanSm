$kind: http-request
url: "{{baseUrl}}/claims/{{claimId}}/review"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "action": "REJECTED",
      "comment": "Receipt is not valid / expired membership"
    }
order: 6000
